## プロジェクト概要

- プロジェクト名は arithprac とする。
- 算数の計算問題を自動生成し，印刷できるようPDFファイルを生成するツールである。
    - 式を出題して解答を書く「暗算」形式
    - 筆算を出題して解答を書く「筆算」形式
    - 暗算形式と筆算形式は出題形式及びレイアウトが全く異なる。
- 「問題」と「解答」を1つのPDFファイル内で順に生成する。
- プログラムは Ruby で書く(4.0 以上想定)。中間形式を .typ (Typst 形式) とし，Typst (1.5 想定)でコンパイルして PDF を生成する。
- Ruby では標準ライブラリ以外の Gem は使用しない。Typst も標準以外のパッケージは使用しない。

## 問題パターン

- 「パターン」として，単問の形式を定める(例: P-1-1-1 など)
- 「ステージ」として，「パターン」の混合比率と問題数を定める(例: たしざん初級1 など)

### パターン: 暗算-加算(P1-1-x)

繰り上がりの有無と，被加数/加数/結果 の範囲や桁数によって分類する。

- P1-1-1: 1..5 + 1..5 = □(2..10)
    - 例1: 1 + 1 = □
    - 例2: 2 + 3 = □
    - 例3: 5 + 5 = □
- P1-1-2: 6..9 + 1..5 = □(7..10)
    - 例1: 6 + 4 = □
    - 例2: 7 + 3 = □
    - 適合しない例1: 5 + 5 = □
    - 適合しない例2: 9 + 6 = □
- P1-1-3: 1..9 + 1..9 = □(2..10)
    - 例1: 3 + 4 = □
    - 例2: 3 + 6 = □
    - 例3: 2 + 8 = □
    - 適合しない例1: 5 + 6 = □
    - 適合しない例2: 9 + 2 = □
- P1-1-4: 6..9 + 2..5 = □(11..14)
    - 例1: 9 + 2 = □
    - 例2: 7 + 4 = □
    - 適合しない例1: 6 + 4 = □
- P1-1-5: 6..9 + 5..9 = □(11..18)
    - 例1: 7 + 8 = □
    - 例2: 9 + 6 = □
    - 適合しない例1: 5 + 7 = □
- P1-1-6: 2..9 + 2..9 = □(11..18)
    - 例1: 8 + 4 = □
    - 例2: 5 + 6 = □
    - 適合しない例1: 1 + 9 = □

### パターン: 暗算-減算(P1-2-x)

TBD

### パターン: 暗算-乗算(P1-3-x)

桁数と数値の範囲によって分類する。

- P1-3-1: 2..9 × 2..9 = □(4..81)
    - 例1: 2 × 3 = □
    - 例2: 5 × 6 = □
    - 適合しない例1: 1 × 9 = □
    - 適合しない例2: 3 × 0 = □
- P1-3-2: 11..99 × 2..9 = □(22..891)
    - 例1: 12 × 3 = □
    - 例2: 45 × 6 = □
- P1-3-3: 11..19 × 11..19 = □(121..361)
    - 例1: 12 × 13 = □
- P1-3-4: 22..99 × 12..19 = □(252..551) ※ただし，'0' の数字は出現しない。被乗数に '1' の数字は1度も出現しない。
    - 例1: 22 × 13 = □
    - 適合しない例1: 91 × 14 = □
    - 適合しない例2: 90 × 16 = □
- P1-3-5: 12..99 × 12..99 = □(144..9801) ※ただし，'0' の数字は1度も出現しない。'1' の数字は4つの数字のうち過不足なく1回出現。
    - 例1: 22 × 13 = □
    - 例2: 12 × 45 = □
    - 適合しない例1: 12 × 14 = □
    - 適合しない例2: 13 × 71 = □
- P1-3-6: 22..99 × 22..99 = □(484..9801) ※ただし，'0' および '1' の数字は1度も出現しない。
    - 例1: 22 × 33 = □
    - 例2: 44 × 55 = □

### パターン: 暗算-除算(P1-4-x)

TBD

### パターン: 筆算-加算(P2-1-x)

TBD

### パターン: 筆算-減算(P2-2-x)

TBD

### パターン: 筆算-乗算(P2-3-x)

TBD

### パターン: 筆算-除算(P2-4-x)

TBD

### ステージ: 暗算-加算

- たしざん暗算1(S1-1-1)
    - 1回あたり10問，同一の問題は各回あたり2回以上出題されない。スケール(``--scale``相当)は `large`。
    - P1-1-1: 10問
    - ただし「1回」あたり，全問を通じて被加数と加数への '1' の出現回数を最大で 2 とする。2 を超える場合，'1' が出現している問題を問題番号の大きいほうから順に1つずつ，'1' を含まないものが出現するまで同一パターンの問題で差し替え続けることを続けて，'1' の回数を調整する。
    - ただし「1回」あたり，全問を通じて被加数と加数への 1 の出現回数を最大で 2 とする。2 を超える場合，'1' が出現している問題を問題番号の大きいほうから順に1つずつ，'1' を含まないものが出現するまで同一パターンの問題で差し替え続けることを続けて，'1' の回数を調整する。
- たしざん暗算2(S1-1-2)
    - 1回あたり10問，同一の問題は各回あたり2回以上出題されない。スケールは `large`。
    - P1-1-1: 2問
    - P1-1-2: 2問
    - P1-1-3: 6問
    - ただし「1回」あたり，全問を通じて被加数と加数への 1 の出現回数を最大で 1 とする。調整方法はS1-1-1と同様とする。
- たしざん暗算3(S1-1-3)
    - 1回あたり10問，同一の問題は各回あたり2回以上出題されない。スケールは `large`。
    - P1-1-1～P1-1-3: 2問(各パターンは等確率)
    - P1-1-4: 8問
    - ただし「1回」あたり，全問を通じて被加数と加数への 1 の出現回数を最大で 1 とする。調整方法はS1-1-1と同様とする。
- たしざん暗算4(S1-1-4)
    - 1回あたり10問，同一の問題は各回あたり2回以上出題されない。スケールは `large`。
    - P1-1-1～P1-1-3: 2問(各パターンは等確率)
    - P1-1-4: 3問
    - P1-1-5: 5問
- たしざん暗算5(S1-1-5)
    - 1回あたり10問，同一の問題は各回あたり2回以上出題されない。スケールは `large`。
    - P1-1-3: 2問
    - P1-1-6: 8問

### ステージ: 暗算-乗算

- かけざん暗算1(S1-3-1)
    - 1回あたり16問(左8問・右8問)。スケールは `medium`。
    - P1-3-1 であるが，問題数・並び順・回数が固定である(CLIオプションでの指定が無視される)。
        - 1回: 2 x 2 = □ ～ 2 x 9 = □ が左側， 3 x 2 = □ ～ 3 x 9 = □ が右側に並ぶ。
        - 2回: 4 x 2 = □ ～ 4 x 9 = □ が左側， 5 x 2 = □ ～ 5 x 9 = □ が右側に並ぶ。
        - 3回: 6 x 2 = □ ～ 6 x 9 = □ が左側， 7 x 2 = □ ～ 7 x 9 = □ が右側に並ぶ。
        - 4回: 8 x 2 = □ ～ 8 x 9 = □ が左側， 9 x 2 = □ ～ 9 x 9 = □ が右側に並ぶ。
- かけざん暗算2(S1-3-2)。スケールは `medium`。
    - 1回あたり20問，同一の問題は各回あたり2回以上出題されない。
    - P1-3-1: 20問
- かけざん暗算3(S1-3-3)。スケールは `medium`。
    - 1回あたり20問，同一の問題は各回あたり2回以上出題されない。
    - P1-3-2: 20問
- かけざん暗算4(S1-3-4)。スケールは `small`。
    - 1回あたり20問，同一の問題は各回あたり2回以上出題されない。
    - P1-3-3: 20問
- かけざん暗算5(S1-3-5)。スケールは `small`。
    - 1回あたり20問，同一の問題は各回あたり2回以上出題されない。
    - P1-3-4: 20問
- かけざん暗算6(S1-3-6)。スケールは `small`。
    - 1回あたり20問，同一の問題は各回あたり2回以上出題されない。
    - P1-3-5: 10問
    - P1-3-6: 10問

## 印刷イメージ

### 共通

- 「問題」は，A4 landscape の中央を点線で区切り，A5 portrait × 2ページ の形式で印刷する。
    - A5 portrait 1つ分を「1回」と呼ぶこととする。
- 「解答」は，A4 portrait の形式で印刷する。

### 問題(暗算) のレイアウト

#### ページ・用紙

- `#set page(paper: "a4", flipped: true, margin: (x: 6mm, y: 8mm))`
    - A4 横(landscape)。マージンは左右 6mm・上下 8mm。
- 1 ページ = A5 × 2枚(= 2回分)。`grid(columns: (1fr, 1fr), column-gutter: 10mm, setA, setB)` で左右に並べる。
- A4 横を左右に分ける中央の切り取り線: `place(top + center, ...)` で高さ 100% の縦線。破線 `dash: "dashed"`，色 `luma(150)`，太さ 0.6pt。
- 全体の本文フォント: `BIZ UDGothic`(定数 `JP_FONT`)，既定サイズ 12pt，`lang: "ja"`。

#### A5 1枚(1回分)の構成 — `probset(title, left, right)`

上から順に:

1. **大見出し**: 中央揃え，18pt，太字(`weight: "bold"`)。文言は「暗算マスター」。
2. `#v(3pt)` の空き。
3. **回・名前・得点の行**: 中央揃え，10pt。
    - 「第N回」(`title`)，`#h(8mm)` の後に「名前」+ 幅 28mm の下線欄(`stroke: (bottom: 0.5pt)`)，`#h(4mm)` の後に「得点」+ 幅 14mm の下線欄。
4. `#v(8pt)` の空き。
5. **問題本体**: 前半(左)と後半(右)の 2 列。`grid(columns: (1fr, 1.5mm, 6mm, 1fr), align: top)`。
    - 左右の間の点線: `grid.vline(x: 2, ...)`，`dash: "dotted"`，色 `luma(140)`，太さ 0.6pt。線をやや左に寄せ，右列の問題番号との隙間を広めにとる(左 1.5mm / 右 6mm)。

#### 1問分の行 — `probrow(n, a, b)`

1 問を 6 セルの表で表す。列幅は `columns: (numw, valw, opw, valw, opw, auto)`，罫線なし(`stroke: none`)，縦中央揃え(`align: horizon`)。行間は `--scale`` 指定により異なる。
    - `small`: `inset: (x: 2pt, y: 6pt)`
    - `medium`: `inset: (x: 2pt, y: 9pt)`
    - `large`: `inset: (x: 2pt, y: 12pt)`

- 列幅定数: `numw = 6mm`(番号)，`valw = 10mm`(被演算数・演算数。3桁でも右揃えで収まる)，`opw = 5mm`(演算子)。
- 各セル:
    1. 問題番号 丸数字(例: ①, ②, ③，⑳，㉑，㉖)— 右揃え，12pt。
    2. 第1項 `a` — 右揃え。フォントサイズは `--scale` 指定により異なる。数値は本文よりやや大きくする。
        - `small`: 13pt とする。
        - `medium`: 14pt とする。
        - `large`: 16pt とする。
    3. 演算子(加算なら `+`)— 中央揃え。フォントサイズは `--scale` 指定により異なる。
        - `small`: 12pt
        - `medium`: 13pt
        - `large`: 15pt
    4. 第2項 `b` — 右揃え。フォントサイズは第1項の数字(`a`)と同一値とする。
    5. 等号「=」— 中央揃え。フォントサイズは第1項の数字(`a`)と同一値とする。
    6. 解答欄 — `align(left + horizon)` に `ansbox` を配置。
- 数値は右揃えで右端をそろえる(将来の桁数拡張に備える)。
- **解答欄 `ansbox`**
    - `box(width: boxw, height: boxh, stroke: 0.7pt, radius: 1pt)`。
    - サイズは `--scale` 指定により異なる。
        - `small`: `boxw = 24mm`(横長の□)，`boxh = 8mm`(手書き用に本文より大きめ)。
        - `medium`: `boxw = 28mm`(横長の□)，`boxh = 12mm`(手書き用に本文より大きめ)。
        - `large`: `boxw = 28mm`(横長の□)，`boxh = 16mm`(手書き用に本文より大きめ)。

#### 前半/後半の振り分け

- 1回 `num` 問を，前半(左)= `left_count(num)` = `(num + 1) / 2` 問(奇数なら左が1問多い)，残りを後半(右)に置く。

### 解答 のレイアウト

問題ページの後に，`#set page` 規則で自動改ページして縦向きの新ページから始める。

#### ページ・用紙

- `#set page(flipped: false, margin: (x: 10mm, y: 10mm))`
    - A4 縦(portrait)。マージンは左右・上下とも 10mm。
- 見出し: 中央揃え，16pt，太字。文言は現行「暗算マスター 解答」。その後 `#v(6pt)`。
- 全 SETS 回分の解答ブロックを横 4 列に配置: `grid(columns: (1fr, 1fr, 1fr, 1fr), column-gutter: 3mm, row-gutter: 6pt)`(横に4回分並べて1ページに多くの回を収める)。

#### 1回分の解答ブロック — `ansblock(title, items, leftn)`

- `block(inset: (x: anspad, y: 6pt), radius: 2pt, stroke: 0.5pt, breakable: false)` の囲み。ブロック幅は内容に合わせて縮める(右側に余白を作らない)。
- 先頭に「第N回」— 太字，11pt。その後 `#v(3pt)`。
- 中身は問題と同様に 前半 / 後半 の縦 2 列: `grid(columns: (auto, anspad, anspad, auto), align: top)`。
    - 区切りの点線は問題と同じ(`dash: "dotted"`，`luma(140)`，0.6pt，`x: 2`)。
    - 左列に `leftn`(= 問題の `left_count`)問，右列に残りを置き，問題と対応させる。
- **すき間の均等化**: 共通すき間 `anspad = 2mm` を，ブロック左右の外側(`inset x`)と中央の点線まわり(グリッドの2列)に同じ幅で適用する。番号を左揃え・答えを右揃えにすることで，次の3箇所がすべて `anspad + セル内側 2pt` で等しくなる。
    - 枠の左線 ↔ 左の丸数字 / 点線 ↔ 右の丸数字 / 右の答え ↔ 枠の右線。

#### 1問分の解答セル — `anscell(it)`

- 2 セルの表 `table(columns: (ansnumw, answ), stroke: none, inset: (x: 2pt, y: 1pt))`。列幅は `ansnumw = 6mm`(番号)，`answ = 12mm`(答え。最大4桁 9801 でも右揃えで収まる)。
    1. 問題番号 丸数字(例: ①, ②, ③，⑳，㉑，㉖)— **左揃え**，解答文字サイズ `ansfs = 10pt`(人間が読みやすい固定サイズ)。
    2. 答え — **右揃え**，10pt。
- 番号は左揃え・答えは右揃えで縦位置をそろえる。

## CLIオプション

以下のCLIオプションを受け付ける(Gemは `optparse` を使用)。

- `-s S`, `--stage S`(String): ステージ名。
    - ステージ名指定が行われた場合は，`--num` `--pattern` `--ratio` の指定は無視される。
    - ステージ名は case-insensitive とする。指定されたステージ名が存在しない場合はエラーとする。
- `-p P`, `--pages P`(Integer): 生成する問題のページ数。1ページあたり2回分が含まれるので，生成される問題は `2 * P` 回分となる。`P` のデフォルト値は `DEFAULT_PAGES`(10)。指定値が0以下の場合はエラーとする。
- `--stage-list`: ステージ名(例: S1-1-1)とサブタイトル(例: たしざん暗算1)の対応の一覧とを表示して終了。
- `--num N`(Integer): 1回あたりの問題数。
    - `N` の範囲は `MIN_PROBLEMS`(2)〜`MAX_PROBLEMS`(26)，デフォルト値は `DEFAULT_PROBLEMS`(20)。範囲外の場合は `abort` で終了。
- `--pattern P`(String): 問題パターン名(例: P1-1-1)。複数指定可。指定が行われた場合は `--stage` 指定が無視される。
    - 問題パターン名は case-insensitive とする。指定されたパターン名が存在しない場合はエラーとする。
- `--ratio R`(Rational): 指定されたパターンの混合比率。`--ratio` オプションを指定する場合は，`--pattern` で指定されたパターンの数と同じ数だけ指定する必要がある。ただし，指定されたRational値の合計が1にならない場合(加算処理はRationalで行う)は，合計が1になるように正規化される。`--pattern` オプションが指定されたが `--ratio` が指定されなかった場合は，指定されたパターンの数だけ等しい比率で割り当てられる。
    - OptionParser.acceptでRationalを定義し，引数をRationalとして扱う。
    - 負の数が指定された場合，または指定値の合計が0になった場合はエラーとする。
    - 実際の問題数への丸め方は，
        - まず各パターンに `(指定比率 * 問題数).truncate` の値(整数)だけ問題数を割り当てる。
        - 未割り当ての問題数がある場合は，
            - 各パターンのその時点での `(指定比率 * 問題数) - 割り当て済み問題数` の値を計算し，その値が正になるパターンのみを対象として，その値の比率を各々の確率として，1つの問題をどれかのパターンに割り当てる。
            - これを未割り当ての問題数がなくなるまで繰り返す。
- `--scale S`(String): 文字および解答欄サイズの指定。`S` は `small` / `medium` / `large` のいずれか(case insensitive)。デフォルトは `small`。各指定値に対するレイアウトは印刷イメージの項に記載。
- `-o O`, `--output O`(String): 出力ファイル名。拡張子は `.pdf` とする。指定された文字列が `.pdf` で終わっていないファイル名(= 拡張子がPDFではない)の場合は，定数 `BASENAME` に `.pdf` を付けたものを使用する。なお，途中で生成される `.typ` ファイルは，出力ファイル名の拡張子を `.typ` に置き換えたものを使用する。
- `--seed S`(Integer): 乱数シード(再現用)。指定時は `srand(seed)` を実行。
- `-h`, `--help`: 使い方を表示して終了。

#### 生成・出力に関する定数(コード内固定値。必要ならオプション化を検討)

- `SETS = 10`: 回数(A5 の枚数)。問題は 2 回分ずつ A4 横 5 ページ，解答は A4 縦 1 ページ。
- `JP_FONT = 'BIZ UDGothic'`: 使用フォント。
- `BASENAME = 'arithprac'`: 出力ファイル名の基底。`arithprac.typ` を生成後，`typst compile` で `arithprac.pdf` を生成する。

#### 出力

- `.typ` を書き出し，`system('typst', 'compile', typ_path, pdf_path)` で PDF 化。成功時は `pdfinfo` でページ数を表示，失敗時は警告して `exit 1`。
