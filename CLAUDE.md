## プロジェクト概要

- プロジェクト名は arithprac とする。
- 算数の計算問題を自動生成し，印刷できるようPDFファイルを生成するツールである。
    - 式を出題して解答を書く「暗算」形式
    - 筆算を出題して解答を書く「筆算」形式
    - 時計の読み取りと書き込みを行う「時計」形式
    - 暗算形式・筆算形式・時計形式は出題形式及びレイアウトが全く異なる。
- 「問題」と「解答」を1つのPDFファイル内で順に生成する。
- プログラムは Ruby で書く(4.0 以上想定)。中間形式を .typ (Typst 形式) とし，Typst (1.5 想定)でコンパイルして PDF を生成する。
- Ruby では標準ライブラリ以外の Gem は使用しない。Typst も標準以外のパッケージは使用しない。

## ドキュメント構成

本仕様は肥大化を避けるため複数ファイルに分割している。CLAUDE.md 本体には概要・横断ルール・共通レイアウト・索引を置き，分厚い定義は `docs/` 以下に分ける。

- **常時ロード(この CLAUDE.md に `@import`)**: CLIオプションと多言語対応。ファイル冒頭付近の import 行で読み込まれる。
    - [docs/cli.md](docs/cli.md): CLIオプション / 生成・出力の定数 / 出力。
    - [docs/i18n.md](docs/i18n.md): 多言語対応(紙面文言・ステージサブタイトル・日本語と構造が変わる箇所)。
- **必要時に開く(索引参照)**: パターン/ステージの定義とレイアウト定義。下記「パターン/ステージ索引」「印刷イメージ」から該当ファイルを開く。

パターン/ステージやレイアウトを追加・変更するときは，下記索引で該当ファイルを特定し，そのファイルのみを編集する。新しい演算グループを追加する場合は `docs/spec/<形式>/` に `p*-*.md` / `s*-*.md` を追加し，索引に1行加える。

## 問題パターン

- 「パターン」として，単問の形式を定める(例: P-1-1-1 など)
- 「ステージ」として，「パターン」の混合比率と問題数を定める(例: たしざん初級1 など)

### パターン/ステージ索引

演算グループ単位の索引。個々の `P1-1-1` … や `P3-3-75` などのリーフは各ファイルを開いて参照する。

#### 暗算(P1-x / P3-3 / S1-x) — `docs/spec/mental/`

| グループ | 内容 | パターン | ステージ |
| --- | --- | --- | --- |
| 加算 | P1-1-x / S1-1-x | [p1-1-add.md](docs/spec/mental/p1-1-add.md) | [s1-1-add.md](docs/spec/mental/s1-1-add.md) |
| 減算 | P1-2-x / S1-2-x | [p1-2-sub.md](docs/spec/mental/p1-2-sub.md) | [s1-2-sub.md](docs/spec/mental/s1-2-sub.md) |
| 乗算 | P1-3-x / S1-3-x | [p1-3-mul.md](docs/spec/mental/p1-3-mul.md) | [s1-3-mul.md](docs/spec/mental/s1-3-mul.md) |
| 除算 | P1-4-x (TBD) | [p1-4-div.md](docs/spec/mental/p1-4-div.md) | — |
| 加減算(混合) | S1-5-x | (P1-1/P1-2/P1-3 を参照) | [s1-5-addsub.md](docs/spec/mental/s1-5-addsub.md) |
| 小数乗算 | P3-3-x / S1-8-x | [p3-3-decimal-mul.md](docs/spec/mental/p3-3-decimal-mul.md) | [s1-8-decimal-mul.md](docs/spec/mental/s1-8-decimal-mul.md) |

#### 筆算(P2-x / S2-x) — `docs/spec/column/`

| グループ | 内容 | パターン | ステージ |
| --- | --- | --- | --- |
| 加算 | P2-1-x / S2-1-x | [p2-1-add.md](docs/spec/column/p2-1-add.md) | [s2-1-add.md](docs/spec/column/s2-1-add.md) |
| 減算 | P2-2-x / S2-2-x | [p2-2-sub.md](docs/spec/column/p2-2-sub.md) | [s2-2-sub.md](docs/spec/column/s2-2-sub.md) |
| 乗算 | P2-3-x (TBD) | [p2-3-mul.md](docs/spec/column/p2-3-mul.md) | — |
| 除算 | P2-4-x (TBD) | [p2-4-div.md](docs/spec/column/p2-4-div.md) | — |

#### 時計(P4-x / S3-x) — `docs/spec/clock/`

| グループ | 内容 | パターン | ステージ |
| --- | --- | --- | --- |
| よみ | P4-1-x / S3-1-x | [p4-1-read.md](docs/spec/clock/p4-1-read.md) | [s3-1-read.md](docs/spec/clock/s3-1-read.md) |

### 重複・類似の回避

各ステージの混合比率・制約とは別に，以下を「似た問題が続かない」ための共通ルールとする。適用範囲が異なるので，暗算形式と筆算形式で分けて定める。

#### 1問内の類似(筆算のみ)

- 被加数と加数が「同じ桁数」かつ「相違する桁が1つ以下」の問題は出題しない(例: 649 + 639，136 + 136，48 + 43)。桁数が異なる場合(例: 924 + 76)は類似とみなさない。
- ステージ指定・パターン指定(`--pattern`)のどちらでも適用する。実装上はステージの制約(コスト関数)の1つとして上限 0 で扱い，同じ判定を問題の生成時にも用いる。
- 暗算形式には現時点では適用せず，従来どおり「同一の問題を1回内で重複させない」のみとする。

#### 回内・回間の重複(筆算のみ)

- 「1回」あたり，2桁以上の同じ数値を2度使わない(被加数/被減数・加数/減数を問わない)。1桁の数値は候補が9通りしかなく，重複しても類似には見えないため対象外とする。
- 「1回」あたり，3桁以上の数値どうしが「同じ桁数」かつ「相違する桁が1つ」になる組合せを作らない。別の問題にある数値どうしも対象とする(例: ⑧の加数が 175 で⑨の加数が 174，③の被加数が 987 で④の被加数が 957)。
    - 2桁は対象外とする。相互に2桁以上異なる2桁の数値は最大10個しか作れず，1回で24個の数値を使う場合には成立しないため。
- 直近3回に出題した問題(被加数/被減数・加数/減数・演算子の組)と，直近3回に出題した2桁以上の数値を避ける。回をまたいだ「1桁違い」は対象外とする(1回内で使い切る数値の量に対して候補が足りないため)。
- 暗算形式には現時点では適用せず，従来どおり「同一の問題を1回内で重複させない」のみとする。

#### 候補枯渇時の緩和

上記を厳密に満たす問題が見つからない場合(パターンの候補が少ない場合に起こる)は，以下の順に条件を緩めて必ず1問を得る。緩和は1問ごとに判定し，可能な限り厳しい条件を維持する。

1. 回内の重複回避(完全一致・1桁違い) + 直近3回の問題・数値の回避(既定)
2. 回内の重複回避(完全一致・1桁違い) + 直近3回の問題の回避(数値の再出現を許す)
3. 回内の重複回避(完全一致・1桁違い)のみ
4. 回内の重複回避(完全一致)のみ(1桁違いを許す)
5. 同一問題の重複回避のみ
6. 重複を許容(最終手段)

## 印刷イメージ

### 共通

- 「問題」は，A4 landscape の中央を点線で区切り，A5 portrait × 2ページ の形式で印刷する。
    - A5 portrait 1つ分を「1回」と呼ぶこととする。
- 「解答」は，A4 portrait の形式で印刷する。
- 問題面の用紙設定(A4 横・マージン 左右 6mm/上下 8mm・中央の切り取り線・本文フォント `JP_FONT`)は，暗算・筆算・時計で共通とする。詳細は各レイアウトファイルの「ページ・用紙」を参照(筆算・時計は「問題(暗算)と同一」)。

### 各形式のレイアウト(索引参照)

- [docs/layout/mental.md](docs/layout/mental.md): 問題(暗算)のレイアウト。
- [docs/layout/column.md](docs/layout/column.md): 問題(筆算)のレイアウト。
- [docs/layout/clock.md](docs/layout/clock.md): 問題(時計)のレイアウト。
- [docs/layout/answer.md](docs/layout/answer.md): 解答のレイアウト(暗算・筆算・時計よみ 共通，および時計かき)。

## 全体仕様(常時ロード)

CLIオプションと多言語対応は以下の import により常時読み込む。

@docs/cli.md
@docs/i18n.md
